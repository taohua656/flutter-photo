workflows:
  flutter-build:
    name: Flutter US ID Photo Build
    environment:
      flutter: 3.41.6
      xcode: 15.4
    scripts:
      - flutter clean
      - flutter pub get
      - flutter analyze
      - flutter build appbundle --release
      - flutter build ipa --release
    artifacts:
      - build/app/outputs/bundle/release/app-release.aab
      - build/ios/ipa/*.ipa